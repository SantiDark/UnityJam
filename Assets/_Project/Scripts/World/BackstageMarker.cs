using UnityEngine;

namespace Subject626
{
    /// <summary>Marca el root del backstage greybox y guarda el spawn del reveal (para escena horneada).</summary>
    public class BackstageMarker : MonoBehaviour
    {
        public Vector3 spawn;
        public float yaw;
    }
}
